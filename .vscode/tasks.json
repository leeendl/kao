{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "cppbuild",
			"label": "build kao",
			"command": "/usr/sbin/g++",
			"args": [
				"-fdiagnostics-color=always",
				"-g",
				"${file}",
				"-o",
				"${fileDirname}/${fileBasenameNoExtension}.out"
				"-ldpp"
			],
			"options": {
				"cwd": "${fileDirname}"
			},
			"problemMatcher": [
				"$gcc"
			],
			"group": "build",
			"detail": "compiler: /usr/sbin/g++"
		}
	]
}